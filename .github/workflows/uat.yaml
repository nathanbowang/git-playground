name: uat # CD artifact to uat
run-name: "Deploy to uat: ${{ github.event.workflow_run.pull_requests[1].title }}"

on:
  # This workflow is triggered when the test pipeline completes:
  workflow_run:
    workflows: [test]
    types: [completed]
    branches: [main]

# Remove concurrency

jobs:
  echo:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    environment: uat
    steps:
      - name: echo
        run: echo "hello from uat"

        #q4
