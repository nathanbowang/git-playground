name: Deploy to the Test environment

on:
  push:
    branches:
      - test

jobs:
  ci:
    uses: ./.github/workflows/ci.yaml

  deploy:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy"
