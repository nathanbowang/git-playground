name: Deploy to the Test environment

on:
  push:
    branches:
      - test

jobs:
#  checkoutArtifact:
#    if: false
#    uses: ./.github/workflows/ci.yaml

  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: 46926ec6ff6ca1e48f0f7d233e55bad9c407c57f-jimmyco-ui-artifact
      - run: ls -al
