name: Deploy to the Test environment

on:
  push:
    branches:
      - test

jobs:
  ci:
    if: true
    uses: ./.github/workflows/ci.yaml

  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy"
