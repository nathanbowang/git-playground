name: Deploy to Test

on:
  push:
    branches:
      - test

jobs:
  build:
    uses: ./.github/workflows/build.yaml

  deploy-to-test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Point the Test environment's URL to the artifact"
