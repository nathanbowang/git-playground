name: Init repo
description: "Init repo"

runs:
  using: "composite"
  steps:
    - name: Cache node modules
      shell: bash
      run: echo "init"
    - name: Install missing dependencies
      shell: bash
      run: echo "install"